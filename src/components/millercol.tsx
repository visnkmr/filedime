import { listen } from "@tauri-apps/api/event";

export default function MillerCol({eachif}){
    // invoke(list)
    return <div>
        <button onClick={
        ()=>
        { 
          
      }
    }>
      {/* <TreesIcon className="h-4 w-4 "/> */}
      {eachif.interfolpath}</button>;
        </div>
}